# RES-DATA frontend environment variables
# Copy this file to `.env` and adjust as needed.

# Host root only — service files add the full /api/... path per endpoint.

# Production API (use once the backend is on the main server):
# VITE_API_BASE_URL=https://res-data.com

# Temporary backend ngrok tunnel:
VITE_API_BASE_URL=https://9a1d-217-53-204-116.ngrok-free.app
